noot

int a, b, c, d,l;
char e;
bool f,g,k;

a := b := 4;
c := b + 3 * 2;

read(d);

print(read(d),read(c),3 * print(4));

if {
		int z; read(z);
		(z > a);
	} then {
		int i;
		i := 4;
		z := 5;
		(z > i);
	}
else
	false
fi;

e := 'e';

a := a + (b := 1) + b;

f := a == 4 && b == 4 || d == 4;

g := {
		int h,i;
		read(h,i);
		(h > i);
	}; // compound expression with type boolean

k := if g == true then {
		int i,j;
		j := 5;
		(j > i);
	}
else
	false
fi; // compound expression used with if statement

while k == true do {
	l := l + 1;
	if l > 10 then { k := false; } fi;
} od;